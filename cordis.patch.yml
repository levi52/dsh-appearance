# dsh-appearance — bundle patch layer (declared by dsh.bundle.patch).
# Applied when a profile lists this package in dsh.profile.bundles; also
# usable directly as a --patch overlay:
#   dsh web --patch ./cordis.patch.yml
- insert:
    - id: ui-appearance
      name: 'dsh-appearance'
